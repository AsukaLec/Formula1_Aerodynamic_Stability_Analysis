# Story Refactoring — Execution Plan (待审查)

> 本文件仅为**规划**。在你审查并确认前，不创建 task 文档、不修改任何 `.tex`、不运行任何实验。
> 依据：`redo_target1.md`（重构规范）+ `story_answers.md` / `story_answers_r2.md`（两轮理解校验）。

---

## 0. 一句话目标

把报告从「多个独立实验的拼盘」重写为「一条围绕**可信代理优化（Trustworthy Surrogate-Driven Optimization）**的调查主线」：精度看似解决问题 → 优化暴露异常 → 逐层排除假设 → 最终用 Landscape Diagnosis 统一溯因。

---

## 1. 核心叙事框架（先定，后落地）

### 1.1 研究问题（统一主线）
> 在受限计算预算下，如何构建一个**可信**的代理模型优化框架——不仅求最优解，更要判断**何时能相信**这个解？

### 1.2 七幕弧线（执行用骨架）
| 幕 | 内容 | 在故事中的作用 |
|:--:|------|------|
| 1 | FIA 限算力 / CFD 昂贵 → 代理模型选型（留 XGBoost + DeepEnsemble，压缩 Ridge/RF/MLP/TabNet） | 看似已解决 |
| 2 | 三类异常：违反物理的超高预测值 / PSO=GA=DE（极差 0.15）/ 多场景同解 | 第一次转折 |
| 3 | Deep Ensemble 量化不确定性与密度 → 高分解落在高 σ、低密度区 | 从「准」到「可信」 |
| 4 | Risk-Sensitive 目标（μ−λσ），定位为**防御机制**而非核心创新 | 防御盲区利用 |
| 5 | 不确定性引导的数据精炼 → 证明**认知盲区存在**（非仅 MSE 下降） | 取证 |
| 6 | Landscape Diagnosis = **核心章节**，统一解释全部异常（景观退化） | 揭根因 |
| 7 | 结论：优化器非瓶颈，数据覆盖/不确定性/景观结构决定上限 | 最终发现 |

### 1.3 叙事顺序决策（采纳 Q14.4 第三方案）
- 章节**顺序按方案 A**（异常 → DeepEnsemble → 风险敏感 → 数据精炼 → Landscape → 结论），保留排除法递进与悬念张力；
- 但加 **foreshadowing + call-back 骨架**：引言/方法概览处**预告**「将以景观视角统一解释异常」；每个中间实验末尾留一句「该缓解未根治，提示更深层结构问题」的钩子；Landscape 章节**回收全部伏笔**。

### 1.4 贡献重构（四条）
1. 构建受限预算下的代理模型驱动优化框架；
2. 引入不确定性感知机制实现**可信优化**；
3. 用数据精炼提升高风险区域建模、**验证认知盲区**；
4. （最重要）Landscape Diagnosis 揭示「数据结构与景观退化而非优化器」主导结果。

### 1.5 不可证伪的不动点主线（防 Landscape 被推翻）
全文最稳主线 = 「比『找到最优解』更根本的是『何时能相信这个最优解』，可信性是第一性约束」。撰写时这条主线**不绑定**任何单一根因，确保即使 Landscape 结论被未来实验削弱，论文仍立得住。

---

## 2. 章节重构映射（现有 11 节 → 七幕）

> 现状：`latex_report/sections/` 00–10 共 11 个 `.tex`；`reports/` 为镜像。

| 现有章节 | 处理动作 | 说明 |
|------|:--:|------|
| 00_cover / 01_abstract | 重写 | 摘要改写为「可信优化」主线 + 不动点结论 |
| 02_introduction | 重写 | 重定义研究问题；拒绝方案A/B；埋下 Landscape 伏笔 |
| 03_dataset | 微调 | 提前点出「数据饱和 / 共线 r=0.895」为后文景观退化埋线 |
| 04_surrogate | **大幅压缩** | 模型大乱斗压成一小节，仅留选型依据；强调「精度高 ≠ 已解决」 |
| 05_pso | 重新解释 | PSO/GA/DE 对比改为「不同优化器同解」的**异常证据**，非「PSO 最好」 |
| 06_active_learning | 重写定位 | 改为「不确定性引导精炼 = 盲区取证」；接入多轮精炼（新实验B） |
| 07_multi_scenario | 重新解释 | 「多场景同解」作为核心异常；接入权重敏感性（新实验C） |
| 08_porpoising | 评估保留/合并 | 与可信主线关系待定（见 §6 决策点） |
| 09_experiments | **升格为核心** | Landscape Diagnosis 提升为破案章；接入去饱和实验（新实验D） |
| 10_conclusion | 重写 | 四条贡献 + 不动点主线 + 反事实稳健性说明 |
| （SHAP 部分） | **降级** | 降为辅助解释模块，加共线性限定（r=0.895，非因果） |

---

## 3. 实验工作清单

### 3.1 保留并强化（仅重定位，无需重跑）
- Deep Ensemble → 可信度度量基石
- Risk-Sensitive PSO → 防御机制
- Data Refinement → 盲区取证
- Landscape Diagnosis → 统一解释框架（核心）

### 3.2 重新解释（无需重跑，仅改叙述）
- PSO/GA/DE 对比（`figures/16_ci_comparison.png` 已有）→ 证明「同解」

### 3.3 压缩
- 模型大乱斗（Ridge/RF/MLP/TabNet）→ 一小节
- SHAP → 辅助模块

### 3.4 新增实验（按优先级；需写代码 + 跑数 + 出图）
| 编号 | 名称 | 支撑命题 | 需新增代码 | 预估 |
|:--:|------|------|------|:--:|
| **D（最高）** | 去饱和景观实验：对目标做非线性变换后重优化，看解是否显著移动 | 「同解源于景观退化，非优化器失效」 | `experiments/run_desaturation.py` | 1–2h |
| **A** | OOD 压力测试：构造分布外输入，比较原代理 vs 风险敏感输出 | 「风险敏感能抑制代理幻觉」 | `experiments/run_ood_stress.py` | 1–2h |
| **C** | 权重敏感性：扫 w1/w2/w3，测最优解位移 | 「景观退化导致权重失效」 | `experiments/run_weight_sensitivity.py` | 1h |
| **B** | 多轮数据精炼：Round1/2/3 边际收益 | 「盲区可被修补，但收益递减」 | 扩展现有精炼脚本 | 1.5–2h |

> 备注：上述脚本是否已部分存在、现有数据能否支撑（尤其 D 的目标变换与重训），将在执行第 1 步先行核查（见 §4 Phase 1）。

---

## 4. 执行阶段划分（含依赖）

```
Phase 0  叙事冻结（本规划获批后）—— 定研究问题/七幕/贡献/叙事骨架，建 T17 任务文档
   ↓
Phase 1  新实验（重写依赖其结果）—— 先核查代码/数据可行性，再按 D → A → C → B 跑
   ↓
Phase 2  出图 —— 为 D/A/C/B 生成统一风格图（接续 figures/ 现有 01–16 编号）
   ↓
Phase 3  章节重写 —— 按 §2 映射逐节改写（02→04→05→06→07→09→10→01/00）
   ↓
Phase 4  压缩 —— 模型大乱斗、SHAP 降级
   ↓
Phase 5  双目录同步 —— reports/ 与 latex_report/ 内容 + 图一致
   ↓
Phase 6  一致性与红线复核 —— 比对 §8 锁定决策、术语、XeLaTeX 可编译
```

依赖要点：Phase 3（09 章/06 章/07 章）依赖 Phase 1 实验产出；Phase 0 不依赖任何运行。

---

## 5. 风险与硬约束

1. **不可推翻的锁定决策**（来自 `next_session.md` §8）：
   - 「主动学习」→「不确定性引导精炼」；「Pareto 前沿」→「经验权衡前沿面」；「闭环」→「单轮反馈」(若做多轮实验B，需把措辞从『单轮』升级为『有限多轮』并同步全篇)；「核心创新」→「核心改进/防御机制」；场景 speed 固定值、wing 下限 20°。
   - 新增实验 B（多轮精炼）**会与「仅单轮」表述冲突**，需作为受控更新点统一修订，且**不得**因此把精炼重新拔高为标准 Active Learning。
2. **双目录同步**：`reports/` 与 `latex_report/` 必须内容一致，避免只改一处。
3. **编译**：XeLaTeX + ctexart；图路径 `figures/xxx.png` 相对路径；新图需同时放入两个 `figures/`。
4. **环境/编码**：脚本优先用 WSL `python3`，`print` 一律英文，随机种子 42。
5. **数据局限**：数据非真实 CFD —— 全篇保持「不外推真实赛车」的限定（与拒绝方案A 一致）。

---

## 6. 需要你决策的问题（执行前必须确认）

1. **实验范围**：四个新实验全做，还是只做最高优先级的 D（+ A）？纯叙事重构（不跑新实验）是否可接受？
2. **重写力度**：在现有 `.tex` 上**就地改写**，还是新建一套章节、保留旧版备份？
3. **Porpoising（08 章）去留**：并入可信主线 / 保留为独立应用案例 / 压缩？
4. **新实验与「单轮」红线**：是否同意把「单轮反馈」升级为「有限多轮（≤3）」并全篇同步？
5. **PPT 是否联动**：报告主线变更后，是否需要后续同步更新答辩 PPT（`next_session.md` 标注 PPT 已定稿）？
6. **输出语言**：最终报告仍为中文；新实验脚本 `print` 英文（沿用 T09 约定）—— 确认沿用。

---

## 7. 验收标准（重构「完成」的定义）

- [ ] 读者读完能复述「可信优化」研究问题与七幕弧线；
- [ ] 每个保留实验都能回答「它在故事里解决什么问题」；
- [ ] PSO/GA/DE、多场景同解被明确叙述为**异常证据**而非性能优胜；
- [ ] Landscape Diagnosis 成为核心章节并回收全部伏笔；
- [ ] 四条贡献 + 不动点主线齐备，且含反事实稳健性说明；
- [ ] §8 锁定决策无一被违反；
- [ ] `reports/` 与 `latex_report/` 同步、XeLaTeX 可编译。

---

**下一步**：等你审查本文件并就 §6 决策点给出意见后，我再据此创建 `docs/tasks/todo/T17_story_refactor.md` 并进入 Phase 0。
